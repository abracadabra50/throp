# Ultra-simple Dockerfile for Railway (avoids OOM)
FROM node:20-alpine

WORKDIR /app

# Copy package files and install production deps only
COPY package*.json ./
RUN npm install --production --no-audit --no-fund

# Copy pre-built dist folder and source
COPY dist ./dist
COPY src ./src

# Expose port
EXPOSE 3001

# Run directly with node
CMD ["node", "dist/api/server.js"]
