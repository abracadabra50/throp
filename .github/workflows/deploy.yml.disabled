# This workflow is disabled - we now use deploy-gcp.yml for deployments
# Renamed to .disabled to prevent it from running
# Original Railway deployment workflow - kept for reference

name: Deploy to Railway (DISABLED)

on:
  push:
    branches: [main, production]
  pull_request:
    types: [opened, synchronize]

env:
  NODE_VERSION: '20'

jobs:
  # Railway deployment jobs disabled - see deploy-gcp.yml for current deployment
